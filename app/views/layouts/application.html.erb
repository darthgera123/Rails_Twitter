<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <!-- similar to jekyll include we write chunks of code elsewhere and can include it here -->
    <%= render 'layouts/render'%>
  </head>

  <body>
    <%= render 'layouts/header'%>
    <div class="container">

    <%= yield %>
    <%= render 'layouts/footer' %>
  </div>
  </body>
</html>
